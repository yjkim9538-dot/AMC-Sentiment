{
  "name": "amc-consensus",
  "private": true,
  "type": "module",
  "description": "AMC 증시 컨센서스 대시보드 — Cloudflare Pages+D1(functions/, lib/) 및 Netlify(netlify/) 배포본 + 정적 프론트(public/)",
  "dependencies": {
    "@netlify/blobs": "^10.0.0"
  }
}
